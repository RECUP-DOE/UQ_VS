#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='recup_prop_pred_as',
    version='1.0.0',
    description='',
    author='',
    author_email='',
    url='',
    install_requires=[],
    packages=find_packages(),
)
